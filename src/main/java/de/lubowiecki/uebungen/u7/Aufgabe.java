package de.lubowiecki.uebungen.u7;

public class Aufgabe {

    /*
    Entwickle eine ProduktFaker-Klasse.
    In getrennte Arrays kommen
    10 x Name
    10 x Beschreibung
    10 x Preis
    Bei Aufruf der fake-Methode wird die Anzahl gewüschter Produlkte vorgegeben.
    Die Methode erzeugt ein Array in passender Größe und befüllt es mit Produkten
    Der Inhalt der Produkte wird nach Zufall aus den Arrays (Titel, Beschreibung, Preis) zusammengestellt.
    Anzahl kann eine einfache Zufallszahl sein.

    ProduktFaker.fake(10) liefet ein Produkt[] mit 10 Produkten zurück.

    // Hilfe
    Radom rand = new Random()
    int zahl = rand.nextInt(10); // 0...9
    name[zahl]

     */

}
